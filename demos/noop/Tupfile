include_rules

CCFLAGS += -I. -I..
LDFLAGS += `pkg-config glew sdl2 gl --libs` -ldemo -lilgraphics -ltgl -lilasset -lilmath -lilinput -lilutil -ldl -lpng -lz

: foreach *.c |> !cc |>
: foreach *.o | $(TOP)/<libs> $(TOP)/tiffgl/libtgl.a |> !ld |> $(TOP)/noop
