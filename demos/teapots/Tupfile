include_rules

CCFLAGS += -I. -I..
LDFLAGS += `pkg-config glew sdl2 --libs` -ldemo -lilgraphics -lilcommon -lilasset -lilmath -lilinput -lilutil -ldl -lpng -lz

: foreach *.c |> !cc |>
: foreach *.o | $(TOP)/<libs> |> !ld |> $(TOP)/teapots
