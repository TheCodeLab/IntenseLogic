include_rules

CCFLAGS += -I. -I..
CXXFLAGS += `pkg-config bullet --cflags`
LDFLAGS += `pkg-config bullet glew sdl2 --libs` -ldemo -lilgraphics -lilcommon -lilasset -lilmath -lilinput -lilutil -ldl -lpng -lz

: foreach *.cpp |> !cxx |>
: *.o | $(TOP)/<libs> |> !ld |> $(TOP)/bouncing_lights
