include_rules

CCFLAGS += -I. -I..
CXXFLAGS += `$(PKG_CONFIG) bullet --cflags`
LDFLAGS += `$(PKG_CONFIG) bullet glew sdl2 --libs` -ldemo -lilgraphics -ltgl -lilasset -lilmath -lilinput -lilutil -ldl -lpng -lz

: foreach *.cpp |> !cxx |>
: *.o | $(TOP)/<libs> $(TOP)/tiffgl/libtgl.a |> !ld |> $(TOP)/bouncing_lights$(PROG_SUFFIX)
